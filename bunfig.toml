# Bun configuration for development and production

[serve.static]
env = "PUBLIC_*"
# Serve from project root so images and other assets are accessible
# Built CSS and HTML are still served from .dev/ when available
